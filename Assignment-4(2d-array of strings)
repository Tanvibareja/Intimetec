#include <stdio.h>

void main()
{
    
    char array[10][30][50];
    int rows;
    printf("enter no. of rows");
    scanf("%d",&rows);
    int cols;
    printf("enter cols");
    scanf("%d",&cols);
    int count=0;
    int a;
    int b;

    
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            printf("Names at (%d,%d): ", i, j);
            scanf("%s", array[i][j]);
        }
    }

    printf("the 2d array of names is:");
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            printf("%s ", array[i][j]);
        }
        printf("\n");
    }

        for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            if(array[i][j][0]=='a' ||array[i][j][0]=='e' ||  array[i][j][0]=='i' || array[i][j][0]=='o' || array[i][j][0]=='u')
            {
                count++;
            }
        }
        printf("\n");
    }
    printf("Number of names starting with a vowel is %d",count);

            int large=0;
            int count1=0;
    for (int i = 0; i < rows; i++) 
        {
        for (int j = 0; j < cols; j++) 
        {
            int count1=0;
            for(int k=0;array[i][j][k]!= '\0';k++)
            {
                count1++;
            }
            if(count1>large)
            {
                large = count1;
                 a=i;
                 b=j;
            }
        }
        }
        printf("\n");



printf("the longest name is: %s",array[a][b]);
        printf("\n");
    }
